import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
  rating: {
    flexDirection: "row",
    alignItems: "center",
    gap: 10,
  },
  ratingText: {
    fontSize: 14,
    fontWeight: "bold",
  },
});
